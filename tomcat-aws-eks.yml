apiVersion: apps/v1
kind: Deployment
metadata:
  name: tomcat-deployment
  labels:
    app: tomcat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tomcat
  template:
    metadata:
      labels:
        app: tomcat
    spec:
      containers:
      - name: tomcat
        image: 612768910590.dkr.ecr.us-west-2.amazonaws.com/tomcat-aws-eks:latest
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: tomcat-service
spec:
  selector:
    app: tomcat
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: tomcat-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /hello
spec:
  rules:
  - host: a056e2a247a8349d58b26433b1e11fe5-1849920046.us-west-2.elb.amazonaws.com
    http:
      paths:
      - path: /hello
        backend:
          serviceName: tomcat-service
          servicePort: 8080
            
